#include <stdio.h>

int main()
{
	int	counter, limit = 50, character;
	int	array[100];

	for (counter = 0; counter <= limit -1 && (character = getchar() != EOF && character != '\n'); counter)
		printf("");

}
